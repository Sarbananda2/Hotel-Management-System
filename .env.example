# Database Configuration
DB_HOST=localhost
DB_PORT=5432
DB_NAME=hotel_db
DB_USER=postgres
DB_PASSWORD=your_db_password_here

# JWT Secret (must be at least 256 bits / 32 characters)
JWT_SECRET=your-secret-key-here-minimum-32-characters-long-for-security

# Application Configuration
APP_PORT=8080
SPRING_PROFILES_ACTIVE=dev

# Default User Passwords (for seed data)
# These are the plaintext passwords that will be hashed with BCrypt:
# admin@example.com: admin123
# frontdesk@example.com: frontdesk123
# housekeeping@example.com: housekeeping123
